<?xml version="1.0"?>

<Configuration>

  <!-- Optional. This represents the default configuration  -->
  <ServerConfiguration host="localhost" embeddedJNDI="true" />
 <LoggerConfiguration file="${openjms.home}/log4j.xml" />  
  <Connectors>
    <Connector scheme="tcp">
      <ConnectionFactories>
        <ConnectionFactory name="ConnectionFactory" />
      </ConnectionFactories>
    </Connector>
    <Connector scheme="rmi">
      <ConnectionFactories>
        <QueueConnectionFactory name="JmsQueueConnectionFactory" />
        <TopicConnectionFactory name="JmsTopicConnectionFactory" />
      </ConnectionFactories>
    </Connector>
  </Connectors>
    
  <!-- Required  -->
  <DatabaseConfiguration>
    <RdbmsDatabaseConfiguration
      driver="org.hsqldb.jdbcDriver"
      url="jdbc:hsqldb:mem:openjms" 
      user="sa" 
      password=""/>
  </DatabaseConfiguration>

  <!-- Required 
    <DatabaseConfiguration>
    <RdbmsDatabaseConfiguration
      driver="org.apache.derby.jdbc.EmbeddedDriver"
      url="jdbc:derby:${openjms.home}\db\openjmsdb;create=true" 
      user="openjms" 
      password="openjms"/>
  </DatabaseConfiguration>
-->    
  <!-- Required -->    
  <AdminConfiguration script="${openjms.home}\bin\startup.bat" />
    
  <!-- Optional. If not specified, no destinations will be created -->
  <AdministeredDestinations>
    <AdministeredTopic name="events">
    </AdministeredTopic>
  
    <!-- AdministeredQueue name="events" /-->
  </AdministeredDestinations>

  <!-- Optional. If not specified, no users will be created -->
  <Users>
    <User name="admin" password="openjms" /> 
    <User name="anonymous" password="" /> 
  </Users>

</Configuration>
